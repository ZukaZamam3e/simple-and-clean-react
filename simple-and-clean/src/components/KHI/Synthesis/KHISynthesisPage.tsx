import { Box } from "@mui/material";

export const KHISynthesisPage = () => {
  return (
    <Box
      sx={{
        backgroundColor: "rgba(0,255,0,.5)",
        display: "flex",
      }}
    >
      TEst
    </Box>
  );
};
